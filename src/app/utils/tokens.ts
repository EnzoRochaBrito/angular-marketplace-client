export const applicationTokens = {
    jwt: {
        ACCESS_TOKEN: 'accessToken',
        /**
         * 5 minutes
         */
        ACCESS_TOKEN_EXPIRATION: 5 * 60
        // refreshToken is httpOnly,
    }
}